# 瞬连调式工具 程序逻辑状态说明书

> 生成日期：2026-05-17
> 分析范围：backend/ 目录下核心源文件, frontend/src/ 核心组件
> 涉及文件：`main.py`, `api_server.py`, `websocket_server.py`, `mikrotik_api.py`, `connection_manager.py`, `api_connection.py`, `launcher.py`, `guardian.py`, `TerminalContext.tsx`, `TerminalPage.tsx`, `App.tsx`

---

## 1. 变量枚举

### 1.1 全局变量（跨模块共享）

| 变量名 | 模块 | 类型 | 有效值 | 初始化位置 | 说明 |
|--------|------|------|--------|-----------|------|
| `discovered_devices` | `api_server.py` | `Dict[str, dict]` | key=MAC地址, value=设备信息 | 模块加载时 `{}` | MNDP发现的设备缓存 |
| `devices_lock` | `api_server.py` | `threading.Lock` | — | 模块加载时 | 保护 `discovered_devices` |
| `api_pool` | `api_server.py` | `Dict[str, MikroTikAPI]` | key=IP, value=已登录API实例 | 模块加载时 `{}` | HTTP API请求连接池 |
| `api_pool_lock` | `api_server.py` | `threading.Lock` | — | 模块加载时 | 保护 `api_pool` |
| `mndp_core` | `api_server.py` | `MNDPCore` | 单例 | 模块加载时 | MNDP广播监听核心 |
| `active_connections` | `websocket_server.py` | `Dict[str, set[WebSocketConn]]` | key=IP, value=WS连接集合 | 模块加载时 `{}` | 活跃的WebSocket连接 |
| `connections_lock` | `websocket_server.py` | `threading.Lock` | — | 模块加载时 | 保护 `active_connections` |
| `connection_filters` | `websocket_server.py` | `Dict[str, dict]` | key=IP, value={topics, level} | 模块加载时 `{}` | 每个连接的日志过滤参数 |
| `device_last_activity` | `websocket_server.py` | `Dict[str, float]` | key=IP, value=unix时间戳 | 模块加载时 `{}` | 设备最后活动时间 |
| `device_watch_tasks` | `websocket_server.py` | `Dict[str, asyncio.Task]` | key=IP, value=keepalive任务 | 模块加载时 `{}` | 每个设备的keepalive心跳任务 |
| `device_api_connections` | `websocket_server.py` | `Dict[str, MikroTikAPI]` | key=IP, value=API实例 | 模块加载时 `{}` | WebSocket层的API连接 |
| `interface_polling_tasks` | `websocket_server.py` | `Dict[str, asyncio.Task]` | key=IP, value=轮询任务 | 模块加载时 `{}` | 接口列表轮询任务 |
| `interface_api_connections` | `websocket_server.py` | `Dict[str, MikroTikAPI]` | key=IP, value=API实例 | 模块加载时 `{}` | 接口轮询的独立API连接 |
| `device_download_status` | `websocket_server.py` | `Dict[str, bool]` | key=IP, value=是否下载中 | 模块加载时 `{}` | 设备下载状态标记 |
| `traffic_managers` | `websocket_server.py` | `Dict[str, TrafficMonitorManager]` | key=IP, value=流量管理器 | 模块加载时 `{}` | 流量监控管理器 |
| `device_offline_flags` | `websocket_server.py` | `Dict[str, bool]` | key=IP, value=True | 模块加载时 `{}` | 设备离线标志（幂等） |
| `log_cache_store` | `websocket_server.py` | `Dict[str, dict]` | key=IP, value=日志缓存 | 模块加载时 `{}` | 日志缓存存储 |
| `log_api_connections` | `websocket_server.py` | `Dict[str, MikroTikAPI]` | key=IP, value=API实例 | 模块加载时 `{}` | 日志读取的API连接 |
| `websocket_event_loop` | `websocket_server.py` | `asyncio.AbstractEventLoop \| None` | 事件循环实例 | `start_websocket_server()` 中设置 | 跨线程调用asyncio |
| `connection_manager` | `connection_manager.py` | `ConnectionManager` | 全局单例 | 模块加载时 | 连接管理器（引用计数） |
| `slsc_monitor_paused` | `api_server.py` | `bool` | True/False | 模块加载时 `False` | SLSC工具监控暂停标志 |
| `_ct_helper_monitor_running` | `api_server.py` | `bool` | True/False | 模块加载时 `False` | ct_helper监控线程运行标志 |
| `rebootLoading` | `RebootPage.tsx` | `boolean` | True/False | 组件初始化 `false` | 前端重启按钮加载状态 |
| `rebootModalVisible` | `RebootPage.tsx` | `boolean` | True/False | 组件初始化 `false` | 前端重启确认弹窗可见状态 |
| `terminal_sessions` | `websocket_server.py` | `Dict[str, dict]` | key=session_id, value={telnet, stop_event, ...} | 模块加载时 `{}` | Telnet终端会话管理 |
| `_terminal_session_id` | `websocket_server.py` | `int` | 递增整数 | 模块加载时 `0` | 终端会话ID计数器 |
| `wsRef` | `TerminalContext.tsx` | `React.MutableRefObject<WebSocket \| null>` | WebSocket实例或null | 组件挂载时 `null` | 终端WebSocket连接引用 |
| `bufferRef` | `TerminalContext.tsx` | `React.MutableRefObject<string>` | 终端输出文本 | 组件挂载时 `''` | 终端输出缓冲（跨页面切换持久化） |
| `isConnected` | `TerminalContext.tsx` | `boolean` | True/False | 组件挂载时 `false` | 终端WebSocket连接状态 |
| `isConnecting` | `TerminalContext.tsx` | `boolean` | True/False | 组件挂载时 `false` | 终端连接中状态 |
| `consecutive_failures` | `launcher.py` | `int` | 0~MAX_CONSECUTIVE_FAILURES | 守护循环 `0` | 连续健康检测失败次数 |
| `restart_times` | `launcher.py` | `List[float]` | unix时间戳列表 | 守护循环 `[]` | 后端重启时间记录（用于频率限制） |

### 1.2 MikroTikAPI 实例变量

| 变量名 | 类型 | 有效值 | 初始化位置 | 说明 |
|--------|------|--------|-----------|------|
| `host` | `str` | 合法IP或域名 | `__init__` | 设备地址 |
| `username` | `str` | 非空字符串 | `__init__` | 登录用户名 |
| `password` | `str` | 任意字符串 | `__init__` | 登录密码 |
| `port` | `int` | 8728/8729 | `__init__` 默认8728 | API端口 |
| `use_ssl` | `bool` | True/False | `__init__` 默认False | 是否使用SSL |
| `socket` | `socket.socket \| None` | 已连接socket或None | `__init__` 为None | TCP连接 |
| `logged_in` | `bool` | True/False | `__init__` 为False | 登录状态 |
| `api_version` | `'rest' \| 'legacy' \| 'legacy_ssl' \| None` | 三种枚举值或None | `login()` 中设置 | 检测到的API版本 |
| `last_response_time` | `float` | unix时间戳 | `__init__` 为当前时间 | 最后响应时间 |
| `_routeros_version` | `str \| None` | 如 '7.20.2' | `_fetch_routeros_version()` | 系统版本 |
| `_routeros_major_version` | `int \| None` | 6/7/... | `_fetch_routeros_version()` | 系统主版本 |
| `_cached_system_info` | `dict \| None` | 系统信息字典 | `get_system_info()` 中设置 | 系统信息缓存 |
| `_cached_identity` | `str \| None` | 设备名称 | `get_identity()` 中设置 | 设备名称缓存 |
| `_cached_system_date` | `str \| None` | 如 'mar/20/2026' | `_get_system_date()` 中设置 | 系统日期缓存 |
| `_cached_date_time` | `float` | unix时间戳 | 模块加载时 0 | 日期缓存时间 |

### 1.3 重要局部变量

| 变量名 | 函数 | 类型 | 说明 |
|--------|------|------|------|
| `device_ip` | `handle_websocket` | `str \| None` | 当前WS连接的设备IP |
| `polling_tasks` | `handle_websocket` | `Dict[str, asyncio.Task]` | 当前连接的所有轮询任务 |
| `stop_events` | `handle_websocket` | `Dict[str, asyncio.Event]` | 当前连接的停止信号 |
| `consecutive_errors` | 各轮询任务 | `int` | 连续错误计数 |
| `consecutive_failures` | `keepalive_task` | `int` | 连续心跳失败计数 |
| `stop_requested` | `handle_ip_addresses_monitor` | `bool` | 是否请求停止 |
| `reconnect_count` | 各监控函数 | `int` | 重连尝试次数 |

---

## 2. 逻辑状态定义

### 2.1 设备连接状态（核心状态机）

| 状态 | 条件 | 说明 |
|------|------|------|
| **DISCONNECTED** | `logged_in=False`, `socket=None`, IP不在 `api_pool` | 设备未连接 |
| **CONNECTING** | `socket` 正在建立，`logged_in=False` | 连接建立中 |
| **AUTHENTICATING** | `socket` 已建立，正在执行登录流程 | 认证中 |
| **CONNECTED** | `logged_in=True`, `socket` 有效, IP在 `api_pool` | 已连接已认证 |
| **OFFLINE** | `device_offline_flags[IP]=True`, 资源已清理 | 设备离线（不可恢复） |
| **RECONNECTING** | `logged_in=False`, 正在尝试重新登录 | 重连中 |

### 2.2 WebSocket 连接状态

| 状态 | 条件 | 说明 |
|------|------|------|
| **WS_INIT** | WebSocket已建立，未收到首条消息 | 等待初始化 |
| **WS_ACTIVE** | 已注册到 `active_connections`, keepalive任务运行中 | 活跃连接 |
| **WS_POLLING** | 至少一个轮询任务在 `polling_tasks` 中运行 | 正在轮询数据 |
| **WS_CLOSING** | 收到stop命令或WebSocket关闭 | 正在关闭 |
| **WS_CLOSED** | `is_ws_closed()` 返回True, 资源已清理 | 已关闭 |

### 2.3 轮询任务状态

| 状态 | 条件 | 说明 |
|------|------|------|
| **POLL_IDLE** | 无轮询任务在运行 | 空闲 |
| **POLL_RUNNING** | 对应任务在 `polling_tasks` 中且未done | 正在轮询 |
| **POLL_ERROR** | `consecutive_errors >= MAX_CONSECUTIVE_ERRORS` | 错误超限 |
| **POLL_STOPPED** | stop_event被set或任务被cancel | 已停止 |

### 2.4 流量监控状态

| 状态 | 条件 | 说明 |
|------|------|------|
| **TRAFFIC_IDLE** | `traffic_managers` 中无该IP | 无流量监控 |
| **TRAFFIC_MONITORING** | `running=True`, `paused=False` | 正常监控 |
| **TRAFFIC_PAUSED** | `running=True`, `paused=True` | 暂停发送 |
| **TRAFFIC_RECONNECTING** | 连接失效，正在重连 | 重连中 |
| **TRAFFIC_STOPPED** | `running=False` | 已停止 |

### 2.5 设备重启状态

| 状态 | 条件 | 说明 |
|------|------|------|
| **REBOOT_IDLE** | `rebootModalVisible=false`, `rebootLoading=false` | 空闲，未触发重启 |
| **REBOOT_CONFIRMING** | `rebootModalVisible=true`, `rebootLoading=false` | 显示重启确认弹窗 |
| **REBOOT_EXECUTING** | `rebootLoading=true` | 正在发送重启命令 |
| **REBOOT_SENT** | 后端返回 success，前端显示成功提示 | 重启命令已发送，设备即将离线 |

### 2.6 终端会话状态

| 状态 | 条件 | 说明 |
|------|------|------|
| **TERM_DISCONNECTED** | `isConnected=false`, `isConnecting=false`, wsRef=null | 终端未连接 |
| **TERM_CONNECTING** | `isConnecting=true`, WebSocket正在打开 | 正在建立WebSocket连接 |
| **TERM_AUTHENTICATING** | WebSocket已建立，正在等待后端Telnet登录完成 | Telnet登录认证中 |
| **TERM_CONNECTED** | `isConnected=true`, Telnet会话已建立 | 终端已连接，可输入命令 |
| **TERM_RECONNECTING** | WebSocket意外断开，前端显示断开状态 | 需要用户手动重新连接 |

---

## 3. 状态转换

### 3.1 设备连接状态转换

| 从状态 | 到状态 | 触发代码 | 条件 | 变量修改 |
|--------|--------|---------|------|---------|
| DISCONNECTED | CONNECTING | `MikroTikAPI.__init__()` + `login()` | 用户发起连接 | `socket` 创建 |
| CONNECTING | AUTHENTICATING | `_try_legacy_login()` | socket.connect()成功 | `socket` 有效 |
| AUTHENTICATING | CONNECTED | `_plaintext_login()` / `_challenge_login()` | 登录响应 `!done` | `logged_in=True`, `api_version` 设置 |
| AUTHENTICATING | DISCONNECTED | 登录失败 | 响应含 `failure`/`bad`/`trap` | `socket` 关闭, `logged_in=False` |
| CONNECTED | OFFLINE | `mark_device_offline()` | 心跳连续失败/连接异常 | `device_offline_flags[IP]=True`, 清理所有资源 |
| CONNECTED | RECONNECTING | 各监控循环中检测到连接断开 | `logged_in=False` 或 socket异常 | 尝试重新login |
| RECONNECTING | CONNECTED | 重连login成功 | `login()` 返回True | `logged_in=True`, `reconnect_count=0` |
| RECONNECTING | OFFLINE | 重连次数达上限 | `reconnect_count > MAX_RECONNECT_ATTEMPTS` | 标记离线 |

### 3.2 WebSocket 状态转换

| 从状态 | 到状态 | 触发代码 | 条件 | 变量修改 |
|--------|--------|---------|------|---------|
| WS_INIT | WS_ACTIVE | `register_connection()` | 首条消息含有效IP | `active_connections[IP]` 添加WS |
| WS_ACTIVE | WS_POLLING | `handle_interface_polling_single_conn()` 等 | 收到start_polling请求 | `polling_tasks` 添加任务 |
| WS_POLLING | WS_ACTIVE | stop事件被set | 收到stop命令 | 取消任务, 从 `polling_tasks` 移除 |
| WS_ACTIVE | WS_CLOSING | WebSocket断开或收到stop | 连接关闭/stop命令 | stop_events全部set |
| WS_CLOSING | WS_CLOSED | `unregister_connection()` + finally块 | 所有清理完成 | 从 `active_connections` 移除 |

### 3.3 流量监控状态转换

| 从状态 | 到状态 | 触发代码 | 条件 | 变量修改 |
|--------|--------|---------|------|---------|
| TRAFFIC_IDLE | TRAFFIC_MONITORING | `TrafficMonitorManager.start_monitor()` | 接口轮询启动 | `running=True`, 创建监控线程 |
| TRAFFIC_MONITORING | TRAFFIC_PAUSED | `TrafficMonitorManager.pause()` | 收到pause_traffic动作 | `paused=True` |
| TRAFFIC_PAUSED | TRAFFIC_MONITORING | `TrafficMonitorManager.resume()` | 收到resume_traffic动作 | `paused=False` |
| TRAFFIC_MONITORING | TRAFFIC_RECONNECTING | `_start_monitor_sync()` 内层循环 | 连续超时>=3 | 关闭旧socket, 尝试login |
| TRAFFIC_RECONNECTING | TRAFFIC_MONITORING | 重连成功 | login成功 | `consecutive_timeouts=0` |
| TRAFFIC_RECONNECTING | TRAFFIC_STOPPED | 重连失败达上限 | `attempt >= max_reconnect_attempts` | `running=False`, 调用 `mark_device_offline()` |
| TRAFFIC_MONITORING | TRAFFIC_STOPPED | `stop_monitor()` / `cleanup_device_resources()` | 用户登出/设备离线 | `running=False`, 关闭API |

### 3.4 设备重启状态转换

| 从状态 | 到状态 | 触发代码 | 条件 | 变量修改 |
|--------|--------|---------|------|---------|
| REBOOT_IDLE | REBOOT_CONFIRMING | 用户点击"重启设备"按钮 | `onClick={() => setRebootModalVisible(true)}` | `rebootModalVisible=true` |
| REBOOT_CONFIRMING | REBOOT_IDLE | 用户点击"取消"按钮 | `onCancel` 回调 | `rebootModalVisible=false` |
| REBOOT_CONFIRMING | REBOOT_EXECUTING | 用户点击"确认重启" | `handleReboot()` 调用 | `rebootLoading=true`, `rebootModalVisible=false` |
| REBOOT_EXECUTING | REBOOT_SENT | `/api/system/reboot` 返回 success | API响应 `status=success` | `rebootLoading=false`, 显示成功消息 |
| REBOOT_EXECUTING | REBOOT_IDLE | API请求失败 | catch异常 | `rebootLoading=false`, `rebootModalVisible=false` |
| REBOOT_SENT | REBOOT_IDLE | 设备离线触发 | keepalive检测到设备离线 | 自动触发断线重连流程 |

### 3.5 终端会话状态转换

| 从状态 | 到状态 | 触发代码 | 条件 | 变量修改 |
|--------|--------|---------|------|---------|
| TERM_DISCONNECTED | TERM_CONNECTING | `connectTerminal(router)` | 用户点击"New Terminal"菜单 | `isConnecting=true`, 创建WebSocket |
| TERM_CONNECTING | TERM_AUTHENTICATING | WebSocket onopen回调 | WebSocket连接到后端 | `wsRef` 设置为新WebSocket |
| TERM_AUTHENTICATING | TERM_CONNECTED | 后端发送 `{type: 'terminal_init', message: '...'}` | Telnet登录完成 | `isConnected=true`, `isConnecting=false` |
| TERM_CONNECTED | TERM_RECONNECTING | WebSocket onclose回调 | 连接意外断开 | `isConnected=false`, 输出缓冲保留在 `bufferRef` |
| TERM_RECONNECTING | TERM_CONNECTING | `connectTerminal(router)` | 用户重新连接 | `isConnecting=true`, 复用输出缓冲 |
| TERM_AUTHENTICATING | TERM_DISCONNECTED | Telnet登录失败 | `=message=...` 含错误信息 | `isConnected=false`, `isConnecting=false`, 关闭WebSocket |
| TERM_CONNECTED | TERM_DISCONNECTED | `disconnectTerminal()` | 用户登出设备 | `isConnected=false`, 清空 `bufferRef`, 关闭WebSocket |

---

## 4. 控制流骨架

### 4.1 程序启动流程

```
launcher.py:main()                          # 启动器（用户双击的入口）
  ├── kill_old_backend_processes()          # 终止旧版本后端进程
  ├── is_port_open(32995)                   # 检测后端是否已运行
  │   ├── 已运行 → is_process_running('shunlian_backend.exe')
  │   │   ├── 是 → 进入守护模式
  │   │   └── 否 → kill_backend() + start_backend() + wait_for_backend()
  │   └── 未运行 → start_backend() + wait_for_backend()
  ├── open_browser()                        # 打开浏览器访问前端
  └── guardian_loop()                       # 进入进程守护循环
       └── [循环] 每10秒:
            ├── is_process_running()         # 检测后端进程是否存活
            ├── is_port_open(32995)          # 检测端口是否监听
            ├── check_health_api()           # 请求 /api/health
            ├── 全部正常 → consecutive_failures=0
            └── 连续3次失败 → restart_backend()
                 ├── kill_backend()          # 终止旧进程
                 ├── start_backend()         # 重新启动
                 ├── wait_for_backend()      # 等待就绪
                 └── 频率限制: 5分钟内最多5次重启

main.py:main()                              # 后端主入口（由launcher启动）
  ├── load_config()                         # 加载 config.yaml
  ├── setup_logging()                       # 配置日志
  ├── check_admin()                         # 检查管理员权限
  └── uvicorn.run(app, ...)                 # 启动 FastAPI
       └── api_server.py:lifespan()         # 应用生命周期
            ├── _terminate_unauthorized_helper()   # 终止ct_helper.exe
            ├── _start_ct_helper_monitor()         # 启动监控线程
            ├── mndp_core.start_listener()         # 启动MNDP UDP监听
            ├── mndp_core.start_auto_discover()    # 启动MNDP自动发现(10s间隔)
            └── run_websocket_server()             # 在新线程启动WebSocket
                 └── websockets.serve(handle_websocket, port=32996)
```

### 4.2 WebSocket 主消息处理

```
handle_websocket(websocket)
  ├── 等待首条消息 → 解析 {ip, username, password, flags...}
  ├── register_connection(websocket, device_ip)
  │
  ├── 分支判断（互斥）:
  │   ├── is_interface_polling → handle_interface_polling_single_conn()
  │   │    ├── 创建 MikroTikAPI + login
  │   │    ├── 创建 TrafficMonitorManager + start_send_task()
  │   │    └── 启动 interface_polling_task_with_traffic_single_conn()
  │   │         └── 循环: get_interface_list() → 推送 → sleep(2s)
  │   │              └── 内嵌: websocket.recv(timeout=2s) 处理IP操作
  │   │
  │   ├── is_wireless_interfaces → handle_wireless_polling_single_conn()
  │   ├── is_wireless_clients → handle_wireless_clients_single_conn()
  │   ├── is_security_profiles → handle_security_profiles_single_conn()
  │   ├── is_ip_addresses → handle_ip_addresses_single_conn()
  │   │    └── 创建IP轮询任务 → ip_addresses_polling_task_single_conn()
  │   │
  │   ├── is_logs → handle_logs_monitor()
  │   │
  │   └── action 分支:
  │        ├── 'start_interface_polling' → 同上
  │        ├── 'add_ip_address' → handle_add_ip_address() (一次性)
  │        ├── 'edit_ip_address' → handle_edit_ip_address() (一次性)
  │        ├── 'delete_ip_address' → handle_delete_ip_address() (一次性)
  │        ├── 'set_device_name' → handle_set_device_name() (一次性)
  │        └── ...
  │
  ├── 创建 keepalive_task() → 存入 device_watch_tasks[device_ip]
  │
  └── 主消息循环: async for message in websocket
       ├── 解析 action / is_ip_addresses
       ├── 分发到对应 handler
       └── finally: 取消所有polling_tasks, 取消watch_task, unregister_connection()
```

### 4.3 Keepalive 心跳

```
keepalive_task(device_ip, username, password, websocket)
  ├── 创建独立 MikroTikAPI + login
  │    └── 失败 → mark_device_offline() → return
  │
  └── 循环 (每3秒):
       ├── 检查 is_ws_closed(websocket) → 是则 break
       ├── heartbeat_api.keepalive_check()
       │    ├── 成功 → consecutive_failures = 0
       │    └── 失败 → consecutive_failures += 1
       │         └── >= 5 → send_device_offline() + mark_device_offline() → break
       └── 发送 {'action': 'ping'} 到前端
```

### 4.4 MikroTikAPI 登录流程

```
MikroTikAPI.login()
  ├── 尝试 Legacy API (port 8728, no SSL)
  │    └── _try_legacy_login(use_ssl=False)
  │         ├── 创建 socket → connect(host, 8728)
  │         ├── 启用 TCP Keepalive
  │         ├── _plaintext_login() → 成功则 return True
  │         └── 失败 → 重建socket → _challenge_login()
  │
  ├── 尝试 Legacy SSL API (port 8729)
  │    └── _try_legacy_login(use_ssl=True)
  │         ├── SSL wrap socket → connect(host, 8729)
  │         └── 同上 plaintext → challenge 流程
  │
  └── 全部失败 → return False
```

### 4.5 MNDP 设备发现

```
MNDPCore.start_listener()
  └── _listener() 线程循环:
       ├── sock.recvfrom(8192) → 解析MNDP包
       ├── _parse_mndp_packet() → 提取 MAC/Identity/IP/Version/Uptime
       └── 存入 discovered_devices[MAC] + 更新 last_seen

MNDPCore.start_auto_discover(interval=10)
  └── 循环: send_discovery_packet() → sleep(10s)
       └── 遍历本地网卡 → 广播 UDP 包到 255.255.255.255:5678 + 239.255.255.255:5678
```

### 4.6 设备离线检测（多路径）

```
路径1: Keepalive心跳
  keepalive_task → consecutive_failures >= 5
    → send_device_offline() → mark_device_offline()

路径2: 接口轮询
  interface_polling_task_with_traffic_single_conn
    → consecutive_errors >= MAX_CONSECUTIVE_ERRORS
    → 推送 device_offline → mt_api.logged_in = False

路径3: HTTP API异常
  _check_connection_error() → 检测到 10054/reset/refused/timed out
    → 清理 api_pool → mark_device_offline()

路径4: 流量监控
  TrafficMonitorManager._start_monitor_sync()
    → 重连失败达上限 → mark_device_offline()

路径5: 设备重启（预期内离线）
  /api/system/reboot → 发送 /system/reboot 命令
    → 设备开始重启，TCP连接断开
    → read_sentence 抛出 ConnectionResetError/OSError
    → 捕获异常，视为正常现象，返回 success
    → 前端显示成功提示 → keepalive检测到离线 → 触发重连弹窗

mark_device_offline(ip):
  ├── 设置 device_offline_flags[ip] = True (幂等)
  ├── cleanup_device_resources(ip)
  │    ├── 停止 traffic_managers
  │    ├── 取消 interface_polling_tasks
  │    ├── 关闭 interface_api_connections
  │    ├── 关闭 device_api_connections
  │    ├── 取消 device_watch_tasks
  │    ├── 关闭 log_api_connections
  │    ├── 清理 log_cache_store
  │    ├── 清理 connection_filters
  │    ├── 清理 device_last_activity
  │    ├── 清理 device_download_status
  │    └── 关闭所有 WebSocket 连接
  ├── 清理 api_pool[ip]
  └── 向所有 WS 发送 {'status': 'device_offline'}
```

### 4.7 设备重启流程

```
前端 RebootPage
  ├── 用户点击"重启设备" → 显示确认弹窗 (rebootModalVisible=true)
  ├── 用户确认 → handleReboot()
  │    ├── POST /api/system/reboot { ip: router.ipAddress }
  │    └── 后端响应 → 显示成功/失败消息
  │
  └── 设备重启后离线 → keepalive检测到 → 触发断线重连弹窗

后端 api_server.py:reboot_system()
  ├── 从 api_pool 获取 MikroTikAPI 实例
  ├── mt_api.write_sentence(['/system/reboot'])
  ├── 尝试 mt_api.read_sentence(timeout=10)
  │    ├── 收到 '!done' → 返回 success
  │    ├── 收到 trap/failure → 返回 error
  │    └── ConnectionResetError/OSError → 视为正常（设备重启断开连接）→ 返回 success
  └── 记录日志: logger.info(f"重启命令响应: {response}")

MikroTik 设备行为
  ├── 接收 /system/reboot 命令
  ├── 返回 '!done' 响应
  └── 立即关闭TCP连接，开始重启流程
       ├── 终止所有运行进程
       ├── 卸载文件系统
       └── 重启系统
```

### 4.8 终端会话流程（New Terminal）

```
架构概述:
  前端 (TerminalPage.tsx) → TerminalContext.tsx (状态管理) → WebSocket → 后端 (websocket_server.py) → Telnet → MikroTik设备
                                                                    ↑
                                                          bufferRef (输出持久化)
  ─────────────────────────────────────────────────────────────────────

前端 TerminalContext (全局单例，跨页面切换保持)
  ├── connectTerminal(router):
  │    ├── 关闭已有连接 (disconnectTerminal)
  │    ├── 创建新 WebSocket → ws://host/ws-terminal/{device_ip}
  │    ├── isConnecting = true
  │    ├── onopen:
  │    │    └── 发送 { action: 'terminal_login', username, password, deviceId }
  │    ├── onmessage:
  │    │    ├── { type: 'terminal_init', message } → isConnected=true, isConnecting=false
  │    │    ├── { type: 'terminal_output', data } → bufferRef.current += data
  │    │    │                                    → 调用 replayCallback(data)
  │    │    └── { type: 'terminal_error', message } → 显示错误, disconnectTerminal
  │    └── onclose:
  │         └── isConnected=false, 保留 bufferRef 内容
  │
  ├── disconnectTerminal():
  │    ├── 关闭 WebSocket (wsRef.current.close())
  │    ├── isConnected=false, wsRef=null
  │    ├── 可选: bufferRef='' (全局缓冲不清空，仅登出时清空)
  │    └── 发送 { action: 'terminal_logout', sessionId } → 后端清理 Telnet
  │
  ├── sendInput(data):
  │    └── wsRef.current.send({ action: 'terminal_input', data })
  │
  ├── onReplayBuffer(callback):
  │    └── 注册 replayCallback, 立即回放 bufferRef 内容
  │
  └── getBuffer():
       └── 返回 bufferRef.current (切换到后端获取全部缓冲)

前端 TerminalPage (UI组件)
  ├── 挂载时: onReplayBuffer → term.write(data)
  ├── term.onData: sendInput(data)
  ├── 清理: term.dispose()
  └── 不管理连接生命周期（由 TerminalContext 管理）

后端 WebSocket 终端处理流程:
  ┌─────────────────────────────────────────────────────────┐
  │ handle_terminal_session(websocket, device_ip, username, password)
  │
  │  ├── 1. 创建 session_id → terminal_sessions[session_id] = {}
  │  │
  │  ├── 2. Telnet 连接 (asyncio):
  │  │    ├── asyncio.open_connection(device_ip, 23)  # Telnet端口23
  │  │    ├── telnet_login():
  │  │    │    ├── 读取 Login: 提示 → 发送 username
  │  │    │    ├── 读取 Password: 提示 → 发送 password
  │  │    │    └── 读取 RouterOS 提示符 → 登录成功
  │  │    │
  │  │    ├── 3. 发送初始化命令:
  │  │    │    ├── send("/terminal/ink")
  │  │    │    ├── send("/terminal/screen")
  │  │    │    └── send("/terminal/word")
  │  │    │
  │  │    └── 4. 发送 terminal_init 消息 → 前端:
  │  │         └── onmessage: { type: 'terminal_init', message: '' }
  │  │
  │  ├── 5. 主循环 (并行两个任务):
  │  │    ├── Task A - 读取 Telnet 输出:
  │  │    │    └── 循环: reader.read(4096)
  │  │    │         ├── 解码: decode_mikrotik_hex_escape(data)
  │  │    │         ├── 过滤 Telnet 协议控制字符 (IAC, DONT, DO, WILL, WONT, SB, SE)
  │  │    │         └── 发送 { type: 'terminal_output', data } → 前端
  │  │    │
  │  │    └── Task B - 接收 WebSocket 输入:
  │  │         └── 循环: websocket.receive_text()
  │  │              ├── 解析 action
  │  │              ├── 'terminal_input' → Telnet writer.write(data)
  │  │              └── 'terminal_logout' → break (结束会话)
  │  │
  │  └── 6. 清理:
  │       ├── 取消所有任务
  │       ├── Telnet writer.close()
  │       ├── 从 terminal_sessions 移除 session
  │       └── 发送 { type: 'terminal_output', data: '\r\n会话已终止\r\n' }
  │
  └── decode_mikrotik_hex_escape(text):
       ├── 正则: r'<([0-9A-Fa-f]{2}(?:\s+[0-9A-Fa-f]{2})*)>'
       ├── 提取十六进制字节序列
       ├── bytes.fromhex(hex_str).decode('utf-8', errors='replace')
       └── 返回解码后文本 (正确显示中文)

MikroTik 设备行为:
  ├── 支持 / 开头的 CLI 命令 (如 /interface print)
  ├── 支持普通命令 (如 ping 8.8.8.8)
  ├── 自动完成 Tab 补全
  ├── 支持 Ctrl+C 中断命令
  ├── 输出包含十六进制转义 (<XX XX XX>)
  └── 会话保持: 持续到用户登出或 WebSocket 断开
```

---

## 5. 关键副作用标注

| 副作用类型 | 位置 | 描述 |
|-----------|------|------|
| **网络请求 - TCP** | `mikrotik_api.py` 多处 | 连接设备 (port 8728/8729)，发送/接收API命令 |
| **网络请求 - HTTPS** | `mikrotik_api.py:_rest_request()` | REST API请求 (port 443) |
| **网络请求 - UDP** | `api_server.py:MNDPCore` | MNDP广播发现 (port 5678)，组播监听 |
| **网络请求 - Telnet** | `websocket_server.py:handle_terminal_session()` | Telnet连接设备 (port 23)，实现终端命令交互 |
| **WebSocket连接** | `TerminalContext.tsx` | 前端与后端建立WebSocket连接，代理Telnet数据 |
| **字符编码转换** | `websocket_server.py / mikrotik_api.py:decode_mikrotik_hex_escape()` | 解码十六进制转义序列 `<XX XX XX>` 为UTF-8中文 |
| **网络请求 - WebSocket** | `websocket_server.py` | 实时数据推送 (port 32996) |
| **文件 I/O** | `main.py:setup_logging()` | 写入日志文件 `%LOCALAPPDATA%/ShunLianTool/shunlian.log` |
| **文件 I/O** | `api_server.py:load_config()` | 读取 `config.yaml` |
| **文件 I/O** | `websocket_server.py:clear_log_cache()` | 删除FTP下载的临时日志文件 |
| **进程管理** | `api_server.py:_ct_helper_monitor_loop()` | 每2秒执行 `taskkill /F /IM ct_helper.exe` |
| **进程管理** | `api_server.py:open_slsc_tools()` | 启动 `ct_helper.exe` 子进程 |
| **设备控制** | `api_server.py:reboot_system()` | 发送 `/system/reboot` 命令重启设备 |
| **全局状态变更** | `api_server.py:api_pool` | 添加/删除设备API连接 |
| **全局状态变更** | `websocket_server.py:device_offline_flags` | 设置离线标志 |
| **全局状态变更** | `websocket_server.py:active_connections` | 注册/注销WebSocket连接 |
| **全局状态变更** | `websocket_server.py:device_watch_tasks` | 创建/取消keepalive任务 |
| **全局状态变更** | `websocket_server.py:traffic_managers` | 创建/停止流量监控 |
| **全局状态变更** | `websocket_server.py:log_cache_store` | 缓存日志数据 |
| **线程创建** | `api_server.py:lifespan()` | MNDP监听线程、自动发现线程、ct_helper监控线程、WebSocket线程 |
| **线程创建** | `websocket_server.py:TrafficMonitorManager` | 每个设备一个流量监控线程 |
| **进程守护** | `launcher.py:guardian_loop()` | 每10秒检测后端健康（端口+API心跳），连续3次失败自动重启 |
| **进程守护** | `launcher.py:restart_backend()` | 终止旧后端进程，重新启动，等待端口就绪 |
| **进程守护** | `guardian.py:ProcessGuardian` | 独立守护进程，可单独运行，功能同launcher.py中的守护逻辑 |
| **健康检查** | `api_server.py:health_check()` | `/api/health` 端点，返回 `{"status":"ok","timestamp":...}` |

---

## 6. JSON 摘要

```json
{
  "variables": {
    "global": {
      "discovered_devices": { "type": "Dict[str, dict]", "init": "{}", "module": "api_server.py" },
      "api_pool": { "type": "Dict[str, MikroTikAPI]", "init": "{}", "module": "api_server.py" },
      "mndp_core": { "type": "MNDPCore", "init": "singleton", "module": "api_server.py" },
      "active_connections": { "type": "Dict[str, set[WebSocketConn]]", "init": "{}", "module": "websocket_server.py" },
      "device_watch_tasks": { "type": "Dict[str, asyncio.Task]", "init": "{}", "module": "websocket_server.py" },
      "device_offline_flags": { "type": "Dict[str, bool]", "init": "{}", "module": "websocket_server.py" },
      "traffic_managers": { "type": "Dict[str, TrafficMonitorManager]", "init": "{}", "module": "websocket_server.py" },
      "log_cache_store": { "type": "Dict[str, dict]", "init": "{}", "module": "websocket_server.py" },
      "connection_manager": { "type": "ConnectionManager", "init": "singleton", "module": "connection_manager.py" }
    },
    "mikrotik_api_instance": {
      "socket": { "type": "socket|None", "values": "connected_socket | None" },
      "logged_in": { "type": "bool", "values": "true | false" },
      "api_version": { "type": "str|None", "values": "'rest' | 'legacy' | 'legacy_ssl' | None" },
      "_routeros_major_version": { "type": "int|None", "values": "6 | 7 | ... | None" }
    }
  },
  "states": {
    "device_connection": ["DISCONNECTED", "CONNECTING", "AUTHENTICATING", "CONNECTED", "OFFLINE", "RECONNECTING"],
    "websocket": ["WS_INIT", "WS_ACTIVE", "WS_POLLING", "WS_CLOSING", "WS_CLOSED"],
    "polling": ["POLL_IDLE", "POLL_RUNNING", "POLL_ERROR", "POLL_STOPPED"],
    "traffic_monitor": ["TRAFFIC_IDLE", "TRAFFIC_MONITORING", "TRAFFIC_PAUSED", "TRAFFIC_RECONNECTING", "TRAFFIC_STOPPED"],
    "reboot": ["REBOOT_IDLE", "REBOOT_CONFIRMING", "REBOOT_EXECUTING", "REBOOT_SENT"],
    "terminal": ["TERM_DISCONNECTED", "TERM_CONNECTING", "TERM_AUTHENTICATING", "TERM_CONNECTED", "TERM_RECONNECTING"],
    "guardian": ["GUARDIAN_STARTING", "GUARDIAN_MONITORING", "GUARDIAN_RESTARTING", "GUARDIAN_FAILED"]
  },
  "transitions": [
    { "from": "DISCONNECTED", "to": "CONNECTED", "trigger": "MikroTikAPI.login()", "condition": "login success", "effects": "logged_in=True, api_version set, api_pool[ip]=api" },
    { "from": "CONNECTED", "to": "OFFLINE", "trigger": "mark_device_offline()", "condition": "heartbeat failures>=5 OR consecutive_errors>=5 OR connection error", "effects": "device_offline_flags[ip]=True, cleanup all resources, send device_offline to WS" },
    { "from": "CONNECTED", "to": "RECONNECTING", "trigger": "monitoring loops", "condition": "logged_in=False or socket error", "effects": "attempt re-login, reconnect_count++" },
    { "from": "RECONNECTING", "to": "CONNECTED", "trigger": "login() retry", "condition": "login success within MAX_RECONNECT_ATTEMPTS", "effects": "logged_in=True, reconnect_count=0" },
    { "from": "RECONNECTING", "to": "OFFLINE", "trigger": "monitoring loops", "condition": "reconnect_count > MAX_RECONNECT_ATTEMPTS", "effects": "mark_device_offline()" },
    { "from": "WS_INIT", "to": "WS_ACTIVE", "trigger": "register_connection()", "condition": "first message contains valid IP", "effects": "active_connections[ip].add(ws)" },
    { "from": "WS_ACTIVE", "to": "WS_POLLING", "trigger": "handle_*_polling()", "condition": "start polling request", "effects": "polling_tasks[name] = task" },
    { "from": "WS_POLLING", "to": "WS_ACTIVE", "trigger": "stop event set", "condition": "stop command received", "effects": "task cancelled, removed from polling_tasks" },
    { "from": "TRAFFIC_IDLE", "to": "TRAFFIC_MONITORING", "trigger": "TrafficMonitorManager.start_monitor()", "condition": "interface polling started", "effects": "running=True, thread created" },
    { "from": "TRAFFIC_MONITORING", "to": "TRAFFIC_PAUSED", "trigger": "pause_traffic action", "condition": "user pauses traffic", "effects": "paused=True" },
    { "from": "TRAFFIC_PAUSED", "to": "TRAFFIC_MONITORING", "trigger": "resume_traffic action", "condition": "user resumes traffic", "effects": "paused=False" },
    { "from": "TRAFFIC_MONITORING", "to": "TRAFFIC_STOPPED", "trigger": "cleanup_device_resources()", "condition": "device offline or user logout", "effects": "running=False, API closed, manager removed" },
    { "from": "REBOOT_IDLE", "to": "REBOOT_CONFIRMING", "trigger": "user clicks reboot button", "condition": "user initiates reboot", "effects": "rebootModalVisible=true" },
    { "from": "REBOOT_CONFIRMING", "to": "REBOOT_EXECUTING", "trigger": "handleReboot()", "condition": "user confirms reboot", "effects": "rebootLoading=true, POST /api/system/reboot" },
    { "from": "REBOOT_EXECUTING", "to": "REBOOT_SENT", "trigger": "api response success", "condition": "reboot command sent", "effects": "rebootLoading=false, success message shown" },
    { "from": "REBOOT_SENT", "to": "REBOOT_IDLE", "trigger": "device offline detected", "condition": "keepalive detects reboot", "effects": "triggers reconnect modal" },
    { "from": "TERM_DISCONNECTED", "to": "TERM_CONNECTING", "trigger": "connectTerminal(router)", "condition": "user opens Terminal page", "effects": "isConnecting=true, create WebSocket" },
    { "from": "TERM_CONNECTING", "to": "TERM_AUTHENTICATING", "trigger": "WebSocket onopen", "condition": "WebSocket connected to backend", "effects": "wsRef set" },
    { "from": "TERM_AUTHENTICATING", "to": "TERM_CONNECTED", "trigger": "terminal_init message", "condition": "Telnet login completed", "effects": "isConnected=true, isConnecting=false" },
    { "from": "TERM_CONNECTED", "to": "TERM_RECONNECTING", "trigger": "WebSocket onclose", "condition": "connection lost unexpectedly", "effects": "isConnected=false, bufferRef preserved" },
    { "from": "TERM_CONNECTED", "to": "TERM_DISCONNECTED", "trigger": "disconnectTerminal()", "condition": "user logs out", "effects": "isConnected=false, bufferRef cleared, WS closed" },
    { "from": "GUARDIAN_STARTING", "to": "GUARDIAN_MONITORING", "trigger": "backend startup success", "condition": "port 32995 open and /api/health returns 200", "effects": "consecutive_failures=0, open browser" },
    { "from": "GUARDIAN_MONITORING", "to": "GUARDIAN_MONITORING", "trigger": "health_check success", "condition": "process alive AND /api/health returns 200", "effects": "consecutive_failures=0" },
    { "from": "GUARDIAN_MONITORING", "to": "GUARDIAN_RESTARTING", "trigger": "consecutive_failures >= 3", "condition": "process dead OR port closed OR /api/health timeout", "effects": "kill_backend(), start_backend(), consecutive_failures=0" },
    { "from": "GUARDIAN_RESTARTING", "to": "GUARDIAN_MONITORING", "trigger": "restart success", "condition": "backend port open within 30s", "effects": "restart_times.append(now)" },
    { "from": "GUARDIAN_RESTARTING", "to": "GUARDIAN_FAILED", "trigger": "restart limit exceeded", "condition": "5 restarts within 300s OR startup timeout", "effects": "log error, reset consecutive_failures" },
    { "from": "GUARDIAN_FAILED", "to": "GUARDIAN_MONITORING", "trigger": "next health check cycle", "condition": "backend recovers", "effects": "consecutive_failures=0" }
  ]
}
```
