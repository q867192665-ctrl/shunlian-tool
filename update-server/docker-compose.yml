version: "3.8"

services:
  update-server:
    build: .
    container_name: mikrotik-update-server
    ports:
      - "32999:32999"
    volumes:
      - update-data:/app/data
    restart: unless-stopped

volumes:
  update-data:
